# Financial Modeling Prep API Key (Shared Fallback)
# Get your API key from: https://financialmodelingprep.com/developer/docs/
# This key is used as a fallback when users don't provide their own keys
# Copy this file to .env and add your actual API key
VITE_FMP_API_KEY=your_api_key_here

# FMP API Key (for backend Lambda - should be same as VITE_FMP_API_KEY)
# This is used by the Lambda function as a fallback
FMP_API_KEY=your_api_key_here

# FMP Proxy Lambda Function URL
# Get this URL from AWS Console after deploying:
# Lambda → Functions → (search for fmp-proxy) → Configuration → Function URL
# Or run: aws lambda get-function-url-config --function-name <function-name> --query "FunctionUrl" --output text
# This URL is used by the frontend to call the Lambda proxy
VITE_FMP_PROXY_URL=https://your-lambda-url.lambda-url.us-east-2.on.aws/

# Email Notifications
# Your email address for receiving notifications
EMAIL_NOTIFICATION_ADDRESS=your-email@example.com
