# Financial Modeling Prep API Key (Backend Fallback)
# Get your API key from: https://financialmodelingprep.com/developer/docs/
# This key is used by the Lambda function as a fallback when users don't provide their own keys
# Copy this file to .env and add your actual API key
FMP_API_KEY=your_api_key_here

# FMP Proxy Lambda Function URL
# Get this URL from AWS Console after deploying:
# Lambda → Functions → (search for fmp-proxy) → Configuration → Function URL
# Or run: aws lambda get-function-url-config --function-name <function-name> --query "FunctionUrl" --output text
# This URL is used by the frontend to call the Lambda proxy
VITE_FMP_PROXY_URL=https://your-lambda-url.lambda-url.us-east-2.on.aws/

# Email Notifications
# Your email address for receiving notifications
EMAIL_NOTIFICATION_ADDRESS=your-email@example.com

# Send Notification Lambda Function URL
# Get this URL from AWS Console after deploying:
# Lambda → Functions → send-notification → Configuration → Function URL
# Or run: aws lambda get-function-url-config --function-name <send-notification-function-name> --query "FunctionUrl" --output text
VITE_NOTIFICATION_FUNCTION_URL=https://your-notification-lambda-url.lambda-url.us-east-2.on.aws/

# SNS Topic ARN for email notifications
# This will be created when you run the setup-sns script
# Format: arn:aws:sns:us-east-2:your-account-id:ModelAppNotifications
SNS_TOPIC_ARN=arn:aws:sns:us-east-2:your-account-id:ModelAppNotifications
